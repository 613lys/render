apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - rendered.yaml
patches:
  - path: vss-patch.yaml
    target:
      group: secrets.hashicorp.com
      version: v1beta1
      kind: VaultStaticSecret
